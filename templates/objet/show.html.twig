{% extends 'base.html.twig' %}

{% block title %}Objet{% endblock %}

{% block body %}
    <h1>Objet</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Reference</th>
                <td>{{ objet.reference }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ objet.description }}</td>
            </tr>
            <tr>
                <th>MontantEstime</th>
                <td>{{ objet.montantEstime }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_objet_index') }}">back to list</a>

    <a href="{{ path('app_objet_edit', {'reference': objet.reference}) }}">edit</a>

    {{ include('objet/_delete_form.html.twig') }}
{% endblock %}
