<form method="post" action="{{ path('app_objet_delete', {'reference': objet.reference}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ objet.reference) }}">
    <button class="btn">Delete</button>
</form>
