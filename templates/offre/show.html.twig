{% extends 'base.html.twig' %}

{% block title %}Offre{% endblock %}

{% block body %}
    <h1>Offre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ offre.id }}</td>
            </tr>
            <tr>
                <th>DateReception</th>
                <td>{{ offre.dateReception ? offre.dateReception|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>NomDonateur</th>
                <td>{{ offre.nomDonateur }}</td>
            </tr>
            <tr>
                <th>PrenomDonateur</th>
                <td>{{ offre.prenomDonateur }}</td>
            </tr>
            <tr>
                <th>TelephoneDonateur</th>
                <td>{{ offre.telephoneDonateur }}</td>
            </tr>
            <tr>
                <th>MailDonateur</th>
                <td>{{ offre.mailDonateur }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ offre.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_offre_index') }}">back to list</a>

    <a href="{{ path('app_offre_edit', {'id': offre.id}) }}">edit</a>

    {{ include('offre/_delete_form.html.twig') }}
{% endblock %}
